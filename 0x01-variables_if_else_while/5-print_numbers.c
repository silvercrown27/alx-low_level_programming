#include <stdio.h>

/**
 * main - this is the main function
 *
 * Return: always 0 if run is success
 */

int main(void)
{
	int i = 0;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf('\n');
	return (0);
}
