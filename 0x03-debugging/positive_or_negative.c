#include "main.h"

/**
 * positive_or_negative - checks the code
 * @n: parameter for the function
 * Return: nothing
 */

void positive_or_negative(int n)
{
	if (n < 0)
	{
		printf("%d is negative", n);
	}
	else
	{
		printf("%d is positive", n);
}
