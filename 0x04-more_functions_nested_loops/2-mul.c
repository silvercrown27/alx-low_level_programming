#include "main.h"

/**
 * mul - chechs the code
 * @a: parameter 1 for the function
 * @b: parameter 2 for the function
 * Return: always 0
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
