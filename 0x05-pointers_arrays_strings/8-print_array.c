#include "main.h"
#include <stdio.h>

/**
 * print_array - chacks the code
 * @a: parameter 1- an array
 * @n: parameter 2- number of samples to print
 * Return: No value
 */

void print_array(int *a, int n)
{
	int i;

	for (i = 0; i < n; i++)
	{
		printf("%d, ", a[1]);
	}
	printf("\n");
}
